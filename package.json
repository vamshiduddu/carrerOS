{
  "name": "careeros",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm --workspace @careeros/api run dev & npm --workspace @careeros/web run dev",
    "dev:api": "npm --workspace @careeros/api run dev",
    "dev:web": "npm --workspace @careeros/web run dev",
    "dev:desktop": "npm --workspace @careeros/desktop run dev",
    "build": "npm --workspace @careeros/api run build && npm --workspace @careeros/web run build",
    "lint": "npm --workspace @careeros/web run lint",
    "typecheck": "npm --workspace @careeros/api run build && npm --workspace @careeros/web run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.6.3"
  }
}
